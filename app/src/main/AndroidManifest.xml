<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.events">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ui.activity.ValidacaoScreenActivity" />
        <activity android:name="ui.activity.QrCodeActivity" />
        <activity android:name="ui.activity.ConfiguracaoActivity" />
        <activity android:name="ui.activity.SelectedEventActivity" />
        <activity android:name="ui.activity.homeEventsActivity" />
        <activity android:name="ui.activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ui.activity.LoginActivity" />
        <activity android:name="ui.activity.TelaInicialActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>